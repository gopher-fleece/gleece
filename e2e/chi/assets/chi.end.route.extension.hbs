w.Header().Set("x-extended", "{{{ OperationId }}}")

{{#each CustomContext}}
    {{#equal Value "MODE"}}
        w.Header().Set("x-mode", "{{{ Options.mode }}}")
    {{/equal}}
    {{#equal Value "LEVEL"}}
        w.Header().Set("x-level", "{{{ Options.value }}}")
    {{/equal}}
{{/each}}
