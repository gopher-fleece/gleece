ctx.Header("x-extended", "{{{ OperationId }}}")

{{#each CustomContext}}
    {{#equal Value "MODE"}}
        ctx.Header("x-mode", "{{{ Options.mode }}}")
    {{/equal}}
    {{#equal Value "LEVEL"}}
        ctx.Header("x-level", "{{{ Options.value }}}")
    {{/equal}}
{{/each}}