{{#each FuncParams}}
	{{#if IsContext}}
		getRequestContext(req)
	{{else}}
		{{#if TypeMeta.IsByAddress}}

		{{else}}
			*
		{{/if}}
		{{ToLowerCamel Name}}RawPtr
	{{/if}}
	{{#unless @last}}, {{/unless}}
{{/each}}