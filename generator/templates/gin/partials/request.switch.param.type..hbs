
{{#equal TypeMeta.Name "string"}}
{{ToLowerCamel Name}} := {{ToLowerCamel Name}}Raw
{{/equal}}

{{#equal TypeMeta.Name "int"}}
  {{ToLowerCamel Name}}Uint64, err := strconv.Atoi({{ToLowerCamel Name}}Raw)
  if err != nil {
	{{> JsonValidationErrorResponse }}
  }
  {{ToLowerCamel Name}} := int({{ToLowerCamel Name}}Uint64)
  // Pass to validator
{{/equal}}

{{#equal TypeMeta.Name "int8"}}
  {{ToLowerCamel Name}}Uint64, err := strconv.ParseInt({{ToLowerCamel Name}}Raw, 10, 8)
  if err != nil {
	{{> JsonValidationErrorResponse }}
  }
  {{ToLowerCamel Name}} := int8({{ToLowerCamel Name}}Uint64)
  // Pass to validator
{{/equal}}

{{#equal TypeMeta.Name "int16"}}
  {{ToLowerCamel Name}}Uint64, err := strconv.ParseInt({{ToLowerCamel Name}}Raw, 10, 16)
  if err != nil {
	{{> JsonValidationErrorResponse }}
  }
  {{ToLowerCamel Name}} := int16({{ToLowerCamel Name}}Uint64)
  // Pass to validator
{{/equal}}

{{#equal TypeMeta.Name "int32"}}
  {{ToLowerCamel Name}}Uint64, err := strconv.ParseInt({{ToLowerCamel Name}}Raw, 10, 32)
  if err != nil {
	{{> JsonValidationErrorResponse }}
  }
  {{ToLowerCamel Name}} := int32({{ToLowerCamel Name}}Uint64)
  // Pass to validator
{{/equal}}

{{#equal TypeMeta.Name "int64"}}
  {{ToLowerCamel Name}}Uint64, err := strconv.ParseInt({{ToLowerCamel Name}}Raw, 10, 64)
  if err != nil {
	{{> JsonValidationErrorResponse }}
  }
  {{ToLowerCamel Name}} := int64({{ToLowerCamel Name}}Uint64)
  // Pass to validator
{{/equal}}

{{#equal TypeMeta.Name "uint"}}
  {{ToLowerCamel Name}}Uint64, err := strconv.ParseUint({{ToLowerCamel Name}}Raw, 10, 32)
  if err != nil {
	{{> JsonValidationErrorResponse }}
  }
  {{ToLowerCamel Name}} := uint({{ToLowerCamel Name}}Uint64)
  // Pass to validator
{{/equal}}

{{#equal TypeMeta.Name "uint8"}}
  {{ToLowerCamel Name}}Uint64, err := strconv.ParseUint({{ToLowerCamel Name}}Raw, 10, 8)
  if err != nil {
	{{> JsonValidationErrorResponse }}
  }
  {{ToLowerCamel Name}} := uint8({{ToLowerCamel Name}}Uint64)
  // Pass to validator
{{/equal}}

{{#equal TypeMeta.Name "uint16"}}
  {{ToLowerCamel Name}}Uint64, err := strconv.ParseUint({{ToLowerCamel Name}}Raw, 10, 16)
  if err != nil {
	{{> JsonValidationErrorResponse }}
  }
  {{ToLowerCamel Name}} := uint16({{ToLowerCamel Name}}Uint64)
  // Pass to validator
{{/equal}}

{{#equal TypeMeta.Name "uint32"}}
  {{ToLowerCamel Name}}Uint64, err := strconv.ParseUint({{ToLowerCamel Name}}Raw, 10, 32)
  if err != nil {
	{{> JsonValidationErrorResponse }}
  }
  {{ToLowerCamel Name}} := uint32({{ToLowerCamel Name}}Uint64)
  // Pass to validator
{{/equal}}

{{#equal TypeMeta.Name "uint64"}}
  {{ToLowerCamel Name}}, err := strconv.ParseUint({{ToLowerCamel Name}}Raw, 10, 64)
  if err != nil {
	{{> JsonValidationErrorResponse }}
  }
  // Pass to validator
{{/equal}}

{{#equal TypeMeta.Name "bool"}}
  {{ToLowerCamel Name}}, err := strconv.ParseBool({{ToLowerCamel Name}}Raw)
  if err != nil {
	{{> JsonValidationErrorResponse }}
  }
  // Pass to validator
{{/equal}}

{{#equal TypeMeta.Name "float32"}}
  {{ToLowerCamel Name}}Float64, err := strconv.ParseFloat({{ToLowerCamel Name}}Raw, 32)
  if err != nil {
	{{> JsonValidationErrorResponse }}
  }
  // Pass to validator
  {{ToLowerCamel Name}} := float32({{ToLowerCamel Name}}Float64)
{{/equal}}

{{#equal TypeMeta.Name "float64"}}
  {{ToLowerCamel Name}}, err := strconv.ParseFloat({{ToLowerCamel Name}}Raw, 64)
  if err != nil {
	{{> JsonValidationErrorResponse }}
  }
  // Pass to validator
{{/equal}}