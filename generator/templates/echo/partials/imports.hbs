import (
	"context"
	"encoding/json"
	"fmt"
	"io"
	"net/http"
	"strconv"
	"strings"
	"reflect"
	"regexp"
	"github.com/go-playground/validator/v10"
	"github.com/labstack/echo/v4"
	"github.com/gopher-fleece/runtime"
	RequestAuth "{{{AuthConfig.AuthFileFullPackageName}}}"
{{#each Controllers}}
	{{{Name}}}Import "{{{PkgPath}}}"
	{{#each Routes}}
		{{#each FuncParams}}
			{{#if TypeMeta.PkgPath}}
				Param{{{UniqueImportSerial}}}{{{UnwrapArrayTypeRecursive Name}}} "{{{TypeMeta.PkgPath}}}"
			{{/if}}
		{{/each}}
		{{#each Responses}}
			{{#if PkgPath}}
				Response{{{UniqueImportSerial}}}{{{UnwrapArrayTypeRecursive Name}}} "{{{PkgPath}}}"
			{{/if}}
		{{/each}}
	{{/each}}
{{/each}}

{{> ImportsExtension }}

)