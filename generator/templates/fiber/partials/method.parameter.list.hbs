{{#each FuncParams}}
	{{#if IsContext}}
		getRequestContext(fiberCtx)
	{{else}}
		{{#if TypeMeta.IsByAddress}}

		{{else}}
			*
		{{/if}}
		{{ToLowerCamel Name}}RawPtr
	{{/if}}
	{{#unless @last}}, {{/unless}}
{{/each}}