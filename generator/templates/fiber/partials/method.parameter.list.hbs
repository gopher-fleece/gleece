{{#each FuncParams}}
	{{#if IsContext}}
		ctx.UserContext()
	{{else}}
		{{#if TypeMeta.IsByAddress}}

		{{else}}
			*
		{{/if}}
		{{ToLowerCamel Name}}RawPtr
	{{/if}}
	{{#unless @last}}, {{/unless}}
{{/each}}