{
	// Use IntelliSense to learn about possible attributes.
	// Hover to view descriptions of existing attributes.
	// For more information, visit: https://go.microsoft.com/fwlink/?linkid=830387
	"version": "0.2.0",
	"configurations": [
		{
			"name": "Debug Main",
			"type": "go",
			"request": "launch",
			"mode": "debug",
			"program": "${workspaceFolder}/main.go", // Path to your main package or entry point
			"args": [], // Add command-line arguments if needed
			"buildFlags": "", // Add build flags if required
			"env": {}, // Add environment variables if needed
			// "envFile": "${workspaceFolder}/.env" // Optional: Path to environment variable file
		}
	]
}